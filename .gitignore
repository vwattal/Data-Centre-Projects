# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/

# Jira token (never commit)
.jira_token

# Runtime SQLite databases
*.db

# HSD2Jira cron state (runtime, not source)
hsd2jira_cron.json
hsd2jira_cron_log.json
hsd2jira_trend_cron.json
hsd2jira_trend_cron_log.json

# Auto-generated status files (runtime)
AUTO_RENEWAL_STATUS.md

# User-editable JSON data (runtime state)
user_data.json
jira_tracking_notes.json
jira_tracking_rules.json

# Teams webhook URL (per-user config)
routes/webhook_config.json

# Log files
*.log
nohup.out
auto_refresh.log
